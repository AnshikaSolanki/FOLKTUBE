extends layouts/main.pug
include mixins/videoBlock

block content
    .home__videos 
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                views:item.views,
                videofile: item.videofile
            })
            